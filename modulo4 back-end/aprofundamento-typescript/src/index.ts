// exercicio 1 ----------------------------
// a) 

/* const minhaString :string = 5
deu erro pois o valor de minhaString é do tipo number */

/* b) const meuNumero : number | string  = "6"
aplicando Union Type para aceitar string e number */

/* enum Cores {Azul = 'Azul', Amarelo = 'Amarelo', Verde = 'Verde', Vermelho = 'Vermelho' }
type Pessoa = {
    nome: string;
    idade: number;
    corFavorita: Cores;
  };

  const Pessoa = (person: Pessoa) => {
    console.log(`Meu nome e ${person.nome}`);
    console.log(`Minha idade e ${person.idade}`);
    console.log(`Cor favorita ${person.corFavorita}`)
  }

  console.log(Pessoa({nome: 'joao', idade: 32, corFavorita: Cores.Vermelho})); */

